export enum TypeNavigation {
  INDEX = 'index',

  ROOT = '/',

  TABS = '(tabs)',

  NOT_FOUND = '+not-found',
}

export enum UserStatus {
  proccessing = 'processing',
  created = 'created',
  lost = 'lost',
  win = 'win',
}
